replicaCount: 1

resources:
  requests:
    memory: "3.2Gi"
    cpu: "1.6"
  limits:
    memory: "8Gi"
    cpu: "4"

ingress:
  enabled: true
  annotations:
    kubernetes.io/tls-acme: "true"
  hostnames:
    - stm-litellm.prod-trd.lsports-gcp.cloud
    - boost-mcp.lsports.eu
    - trade-mcp.lsports.eu
  hosts:
    - host: stm-litellm.prod-trd.lsports-gcp.cloud
      paths:
        - path: /
          backend:
            servicePort: 4000
    - host: boost-mcp.lsports.eu
      paths:
        - path: /
          backend:
            servicePort: 4000
    - host: trade-mcp.lsports.eu
      paths:
        - path: /
          backend:
            servicePort: 4000

ingress_route:
  enable: true
  port: 4000
  host: "boost-mcp.lsports.eu"
  override_match: "(Host(`boost-mcp.lsports.eu`) || Host(`trade-mcp.lsports.eu`) || Host(`stm-litellm.prod-trd.lsports-gcp.cloud`)) && (PathPrefix(`/`))"
  customHeaders:
    X-Forwarded-Proto: https

gke:
  istio:
    services:
      - hosts:
          - stm-litellm.prod-trd.lsports-gcp.cloud
          - boost-mcp.lsports.eu
          - trade-mcp.lsports.eu
        sticky:
          enabled: true
        port: 4000
        gateway: "istio-ingress/istio-gateway-public"
        name: stm-litellm

autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 2
  targetCPUUtilizationPercentage: 70
  targetMemoryUtilizationPercentage: 70

affinity:
  nodeAffinity:
    requiredDuringSchedulingIgnoredDuringExecution:
      nodeSelectorTerms:
        - matchExpressions:
            - key: instance.type
              operator: In
              values:
                - general

envSecretGcp:
  LITELLM_MASTER_KEY: _manual_stm_litellm_master_key
  DATABASE_URL: _manual_stm_litellm_database_url
  LITELLM_LICENSE: _manual_stm_litellm_license
